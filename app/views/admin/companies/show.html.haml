%h1= t Company.model_name.human
%table.table.table-striped
  %tbody
    - @company.attributes.each do |key, value|
      - if key == 'id' || key.include?('digest') || key == 'created_at' || key == 'updated_at'
        //skip these attributes
      -else
        %tr
          - if key == 'status'
            %td= t 'status.status'
            %td= status_mark @company.status
          -else
            %td= t "activerecord.attributes.company.#{key}"
            %td= value

.form_actions
  .row
    .col-md-2
      = link_to t('actions.back'), admin_companies_path, class: 'btn btn-primary btn-sm'
